[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

# React Router: qualquer rota nao encontrada cai no index.html (SPA)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
